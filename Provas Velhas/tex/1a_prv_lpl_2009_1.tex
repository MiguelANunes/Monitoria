\documentclass[12pt, a4paper]{article}\usepackage{t1enc}\usepackage[latin1]{inputenc}\usepackage[brazil,portuges]{babel}\usepackage{amsmath}\usepackage{amsfonts}\usepackage{amssymb}%\usepackage{graphicx}\topmargin       -1cm \headheight      17pt \headsep  1cm\textheight      24cm\textwidth       16.7cm\oddsidemargin   -2mm\evensidemargin  -2mm\pagestyle{empty}\begin{document}\begin{center}\framebox[\textwidth][c]{$1a.$ Lógica e Programação em Lógica  (LPL) -Joinville, \today}%%\newline\end{center}\vskip1cm Aluno(a): \hrulefill%%%\noindent\begin{enumerate}\setlength{\itemsep}{-5pt}
\item Construa as proposições e faça a interpretação do valor lógico ($\Phi (...)$) de:\begin{enumerate}\setlength{\itemsep}{-5pt}\item $4+4=8$ se somente se $5^3=125 $ então é uma verdade;\item $3^2 + 4^2 = 5^2$ se somente se $\pi $ é  não  for irracional;
\item $5^2=25$ ou  $\pi $ é irracional;
\item $5^3=124 $ e  $3+4=8$ então são falsos.

\end{enumerate} \item Identificar e simbolizar as seguintes proposições matemáticas: \begin{enumerate}\setlength{\itemsep}{-5pt} \item ``{\em x é maior que 5 e maior que 4 ou x não é igual a 6}''; \item ``{\em Se x é menor que 5 e maior que 3, então x éigual a 4}'';\item ``{\em  É falso que Carlos fala inglêsou alemão, mas que não fala francês}''.\item ``{\em Se lógica é importante e avida também, então devo estudar lógica}''.\end{enumerate}\item Construindo a Tabela Verdade,identifique se a fórmula é tautol\'{o}gica,contingente (satisfat\'{i}vel,consistente), ou inv\'{a}lida(contraditória, insatisfatível):\begin{enumerate}\setlength{\itemsep}{-5pt}%\item $(\sim A \leftrightarrow B) \vee  (A  \wedge B)$%\item $(A \vee  \sim B) \rightarrow (A \wedge  B)$\item $(B \rightarrow A)\rightarrow (A \rightarrow  \sim B)$\item $(A \rightarrow (\sim A \rightarrow B)) \rightarrow  B $\item $(B \rightarrow  A) \rightarrow  (A  \rightarrow \sim B)  $\end{enumerate}\item Demonstre se as fórmulas abaixoapresentam implicações lógicas:\begin{enumerate}\setlength{\itemsep}{-5pt}\item $q \Rightarrow p \wedge q\leftrightarrow q$\item $ (x=y \vee x < 4) \wedge x \geq 4\Rightarrow x=y $\item $(x \neq 0 \rightarrow x=y) \wedge x\neq y \Rightarrow x=0 $\end{enumerate}\item Demonstre se as fórmulas abaixoapresentam  equivalências lógicas:\begin{enumerate}\setlength{\itemsep}{-5pt}\item $p \leftrightarrow p \wedge q \Leftrightarrowp \rightarrow q$\item $(p \rightarrow q) \wedge (p \rightarrow r) \Leftrightarrow p \rightarrow q \wedge r $
\item $(p \rightarrow q) \vee (p \rightarrow r) \Leftrightarrow p \rightarrow  q \vee r $
\end{enumerate}
\item Encontre as  Formas Normais (FN) abaixo e as respectivas fórmulas
duais de cada FN abaixo:\begin{description}\setlength{\itemsep}{-3pt} \item [Disjuntiva] para: $(p \rightarrow q) \wedge \sim(q \rightarrow p) $ \item [Conjuntiva] para: $\sim (\sim p \rightarrow q) \vee (q \rightarrow \sim p) $\end{description}
\item Verificar a validade dosargumentos que se seguem:\begin{enumerate}\setlength{\itemsep}{-2pt} \item $p\rightarrow \sim q$, $\sim p \rightarrow (r \rightarrow \sim q)$, $(\sim s \vee \sim r)\rightarrow \sim \sim q$, $\sim s$ $\vdash $ $\sim r$\item $\sim p\vee q \rightarrow r$,  $r \vee s\rightarrow \sim t$, t {\bf $\vdash $} $\sim q$\end{enumerate}
%%%%%%%%%%%%%%%%% fim 
\end{enumerate}\end{document}